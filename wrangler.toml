name = "expense-tracker"
main = "worker/index.ts"
compatibility_date = "2026-06-01"

# Serve the built React SPA as static assets; the Worker handles /api/*.
[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
